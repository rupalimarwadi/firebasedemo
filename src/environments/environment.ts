// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCckT58XLS8m6L60hLeToMKS6UZR-GKknU",
    authDomain: "fir-demo-cebb5.firebaseapp.com",
    databaseURL: "https://fir-demo-cebb5.firebaseio.com",
    projectId: "fir-demo-cebb5",
    storageBucket: "fir-demo-cebb5.appspot.com",
    messagingSenderId: "459973876722",
    appId: "1:459973876722:web:5de7613e8237e97abbe96b",
    measurementId: "G-600FRYBD5E"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
